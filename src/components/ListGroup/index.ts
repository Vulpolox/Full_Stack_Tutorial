// if given a folder in an import statement, the compiler will look for an 'index' folder
// within that folder and supply the imports from that

import ListGroup from './ListGroup';

export default ListGroup